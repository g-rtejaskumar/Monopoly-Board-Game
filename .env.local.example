# Local development frontend WebSocket endpoint
# (build-time Vite variable — see .env.example for the full explanation)
VITE_WS_URL=ws://localhost:8787/ws

# Local backend port (Render injects PORT automatically in production)
PORT=8787
