# Replace this placeholder in Vercel Environment Variables
# with the real Render WebSocket endpoint.
# Must use wss:// (TLS) and include the /ws path.
VITE_WS_URL=wss://YOUR-RENDER-SERVICE.onrender.com/ws
